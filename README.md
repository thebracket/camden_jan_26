![](./manual/src/images/ardanlabs-logo.png)

You can build a local copy of this training manual:

1. Run `cargo install mdbook`.
2. From the `manual` directory, run `mdbook serve`.
3. Open your browser to `http://localhost:3000`.
