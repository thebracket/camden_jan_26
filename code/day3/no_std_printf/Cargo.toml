[package]
name = "no_std_printf"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = { version = "0.2", default-features = false }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

